apiVersion: v1
kind: Service
metadata:
  name: app-service
  namespace: vegait-training
spec:
  type: ClusterIP
  selector:
    app: app
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 3000
